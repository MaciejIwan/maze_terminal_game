#if !defined(_CONNECTION_H_)
#define _CONNECTION_H_
#include "common.h"

void connection_init(); // for server to setup connection files etc
// static int connection_client_setup();
// static int connection_server_setup();
#endif // _COMMON_H_